# Reports

Published artifacts from the VBN-analysis pipeline, session `1055240613`.

## Start here
- **[NB08_FULL_ANALYSIS.md](NB08_FULL_ANALYSIS.md)** — full findings, method changes, post-critique addendum.
- **[YC_SESSION_EXPORT.md](YC_SESSION_EXPORT.md)** — narrative session summary (bug catch + multi-agent critique arc).
- **[Z2_critique_resolution_summary.png](Z2_critique_resolution_summary.png)** — 6-panel before/after figure.

## Per-deliverable artifacts
- **A** — Active vs Passive per-unit modulation: `A_*` (original), `A2_*` (arousal-matched post-critique).
- **B** — Change-detection hierarchy: `B_*` (original), `B2_*` (lick-controlled + miss-only post-critique).
- **C** — Pre-stim arousal → hit/miss: `C_*`.
- **D** — Per-area encoding R² + variance partitioning: `D_*` (original), `D2_*` (with saccades + pupil_vel + gap_bins fix).
- **F** — Flash-onset latency hierarchy: `F_*`.

## Master tables
- **[Z2_before_after_master.csv](Z2_before_after_master.csv)** — per-area consolidated before/after deltas.
